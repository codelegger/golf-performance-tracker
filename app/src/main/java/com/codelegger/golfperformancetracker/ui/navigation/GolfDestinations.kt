package com.codelegger.golfperformancetracker.ui.navigation

/**
 * Type-safe-ish route catalog for the app's navigation graph.
 *
 * Only [HOME] exists in the foundation slice. Player list, player detail, and shots
 * destinations are added as their feature slices land.
 */
object GolfDestinations {
    const val HOME = "home"
}
